*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
*.apk
*.aab

# Release signing: keystore and its credentials are never committed
keystore.properties
*.jks
*.keystore

# Gradle build output in any module
build/

# ML model files are downloaded at runtime, never committed
/app/src/main/assets/models/

# Spike model artifacts (large, regenerated)
spikes/clip/out/
.testdata/
__pycache__/

# Eval datasets (downloaded, not committed — see scripts/eval/)
.evaldata/
eval-out/
